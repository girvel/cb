# cb

Clipboard convenience wrapper around xclip, depends on it.

```bash
./cb  # displays the clipboard
echo 123 | ./cb  # writes to & displays the clipboard
```

Not really useful, you can move on.
